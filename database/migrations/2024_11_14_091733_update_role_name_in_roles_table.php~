<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\DB;

class UpdateRoleNameInRolesTable extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        // Update role name from 'admin' to 'author'
        DB::table('role')->where('role', 'admin')->update(['role' => 'author']);
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        // Revert role name from 'author' back to 'admin'
        DB::table('role')->where('role', 'author')->update(['role' => 'admin']);
    }
}
